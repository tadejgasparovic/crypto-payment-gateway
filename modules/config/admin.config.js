module.exports = {
	apiBase: "https://127.0.0.1:3200", // payment-gateway API base (must be accessible from the end user's browser)
	dateFormat: "MM/dd/yyyy hh:mm:ss", // Default date & time format
};
